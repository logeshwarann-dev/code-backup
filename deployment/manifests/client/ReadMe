helm ls --all-namespaces
# OR
helm ls -A

# Delete release
helm uninstall release_name -n release_namespace


#Load image into minikube
eval $(minikube docker-env)
docker build --compress -t <image-name> .